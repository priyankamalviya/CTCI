/* Write a SQL query to get a list of tenants who are renting more than 1 apartment.
    Given: 2 tables: Apartments and AptTenants */


SELECT TenantName
FROM Tenants
INNER JOIN (
            SELECT  TenantID
            FROM AptTenants 
            GROUP BY TenantID
            HAVING count(*) > 1
            ) A
ON Tenants.TenantID = A.TenantID
